= render layout: 'tenon/fields/base', locals: { f: f, method_name: method_name, value: value, opts: opts } do
  = f.super_radio_button method_name, value, opts.merge(class: 'input-block__radio-button')
