#asset-uploader
  #progress
  = form_for Tenon::Asset.new, format: 'json', builder: Tenon::FormBuilder do |f|
    = error_messages_for :asset
    .fields.full
      .form-group.full
        = f.text_field :title, placeholder: '--', explanation: "#{t('tenon.asset.enter_a_title_for_your_asset')} #{defined?(multiple) ? t('tenon.asset.multiple_assets') : '' }"

      .form-group
        .browse-button-holder
          .fileupload.btn.btn-comp.browse
            %span#choose-files= defined?(multiple) ? t('tenon.asset.choose_file') : t('tenon.asset.choose_files')
            %input{ :type => 'file', :class => 'upload', multiple: defined?(multiple) && multiple, style: 'width: 100%;', name: 'asset[attachment]' }

    .spacer

