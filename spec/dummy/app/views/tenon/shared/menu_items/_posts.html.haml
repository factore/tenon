- if policy(Post).index?
  = tenon_menu_item item, posts_path, :active => is_active(["posts", "post_categories"]), :submenu => policy(Post).create? || policy(PostCategory).index?
  - if policy(Post).create? || policy(PostCategory).index?
    %ul.tenon_submenu{ :class => is_active(["posts", "post_categories"]) ? "#{item} active" : item }
      = link_to "New Post", new_post_path, :class => ("active" if params[:controller] == "tenon/posts" && params[:action] == "new") if policy(Post).create?
      = link_to "Post Categories", post_categories_path, :class => ("active" if params[:controller] == "tenon/post_categories") if policy(PostCategory).index?
    .spacer
